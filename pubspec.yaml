name: chatbot
description: "EduPath AI - Chatbot Tư Vấn Hướng Nghiệp THPT (Flutter App)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.7.3
  http: ^1.2.2
  shared_preferences: ^2.3.2
  google_fonts: ^6.2.1
  supabase_flutter: ^2.8.0
  file_picker: ^8.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
